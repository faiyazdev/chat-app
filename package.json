{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --production=false --prefix frontend && npm install --production=false --prefix backend && npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:push": "npm run db:push --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}
